import { FilterTransactionIdPipe } from './filter-transaction-id.pipe';

describe('FilterTransactionIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTransactionIdPipe();
    expect(pipe).toBeTruthy();
  });
});
